import React from "react";

const DocumentViewerLayout = ({ children }) => (
  <div className="min-h-screen bg-[#f8f9fa] flex flex-col">
    {children}
  </div>
);

export default DocumentViewerLayout;